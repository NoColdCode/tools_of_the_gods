package net.mcreator.toolsofthegods.procedures;

import net.minecraft.world.entity.Entity;

public class PrimalWoodenToolsPickaxeToolInInventoryTickProcedure {
	public static void execute(Entity entity) {
		if (entity == null)
			return;
		
		// Future: Add passive effects based on tier here
		// For now, just empty - we'll add tier-specific effects later
	}
}